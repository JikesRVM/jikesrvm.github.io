LaTeX sources are written using normal LaTeX.

The PDF can be build using normal LaTeX tools. There is currently no script to build the PDF.

You can either
a) build a plain HTML userguide for local debugging or
b) build a userguide for Jekyll and update the (local copy of the) website with it