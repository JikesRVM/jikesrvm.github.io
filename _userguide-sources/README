LaTeX sources are written using normal LaTeX.

To build the PDF, use standard LaTeX tools or the script buildPDFUserGuides.sh . The script produces an A4 and a letter PDF version using latexmk.

To build the HTML versions, you can use
a) buildHTMLUserguideForLocalDebugging.sh to build a plain HTML userguide (e.g. for local debugging)
b) updateWebsiteWithNewUserGuide.sh to build a userguide for Jekyll and update the (local copy of the) website with it
